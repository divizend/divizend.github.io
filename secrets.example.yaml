# Example secrets file - copy this to secrets.yaml and fill in your values
# Then encrypt it: sops -e secrets.yaml > secrets.encrypted.yaml

BENTO_API_URL: "http://localhost:4195"
S2_BASIN: ""  # Optional: will be derived from BASE_DOMAIN if empty
BASE_DOMAIN: "example.com"
S2_ACCESS_TOKEN: "your-s2-access-token-here"
RESEND_API_KEY: "re_your-resend-api-key-here"

