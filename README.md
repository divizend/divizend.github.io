Just this on a standard Ubuntu server:

```
curl -fsSL https://setup.divizend.com/setup.sh | bash
```

Leitfrage: Wer muss wann was schicken?

reverser: email => email.plainText.split("").reverse().join("")
